# --- Sensitive project documentation (NEVER commit; repo is public) ---
# These docs contain pricing strategy, legal posture rationale, persona PII,
# competitor analysis, and the UPL-defense playbook. Keep local only.
PLAN.md
/brand/
/docs/
/docs/**
/private/
/private/**
# Operator playbook entry point — auto-loads locally; references the gitignored
# docs/ runbook, so keep it local too (the repo is public).
CLAUDE.md

# --- Environment / secrets ---
.env
.env.*
!.env.example
.continueignore

# --- Node / Next.js ---
node_modules/
.next/
out/
dist/
*.tsbuildinfo
next-env.d.ts

# --- Vercel ---
.vercel/

# --- Astro generated content-types cache (regenerated by astro check/build) ---
.astro/

# --- Supabase local dev ---
/supabase/.branches/
/supabase/.temp/
**/supabase/.env

# --- Logs ---
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# --- Editor / OS ---
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini

# --- Build artifacts that might leak source structure ---
coverage/
.cache/
.turbo/

# --- Local-only state authoring scratch ---
/local-scratch/
*.local.md

# --- Internal go-live tracker (operator-only) ---
TODO.md

# --- Search-performance exports (GSC/Bing) — local-only, never commit (public repo). One zip ~monthly. ---
/gsc-exports/

# --- Visibility/SEO audit report (internal strategy; pricing + competitive posture) — local-only ---
VISIBILITY_AUDIT.md

# --- OpenPGP project keyring + secret-key backups (NEVER commit) ---
/_secrets/

# --- URL-verification tool: screenshots + scraped .gov content, operator-only (public repo) ---
/url-verify/
